[project]
name = "reliability-agent-cli"
version = "0.1.0"
description = "Local agents-cli playground front-end for reliability-mcp-v2."
requires-python = ">=3.10"
dependencies = [
  "google-adk>=1.33.0",
  "litellm>=1.40.0",
]

[tool.agents-cli]
agent_directory = "reliability_agent"
