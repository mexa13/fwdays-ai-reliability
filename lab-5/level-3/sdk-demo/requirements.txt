# google-adk pinned to the version used elsewhere in this repo (lab-3 / lab-4).
google-adk==1.19.0

# Agent Sandbox Python SDK. Pulled from the upstream git tag chosen below.
# Override via `pip install ... agent-sandbox.git@<tag>#subdirectory=...`.
git+https://github.com/kubernetes-sigs/agent-sandbox.git@main#subdirectory=clients/python/agentic-sandbox-client

# OTLP exporter so the ADK side also lands in Phoenix.
arize-phoenix-otel>=0.6.0
openinference-instrumentation-google-genai>=0.1.0
opentelemetry-sdk>=1.27.0
